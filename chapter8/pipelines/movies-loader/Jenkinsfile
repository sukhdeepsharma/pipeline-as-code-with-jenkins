def imageName = 'mlabouardy/movies-loader'
 
node('workers'){
    stage('Checkout'){
        checkout scm
    }
 
    stage('Unit Tests'){
        sh "docker build -t ${imageName}-test -f /chapter8/pipelines/movies-loader/Dockerfile.test ."
        sh "docker run --rm ${imageName}-test"
    }
}